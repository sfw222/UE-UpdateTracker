# For GitHub API
PyGithub

# 用于智谱 GLM API（兼容 OpenAI 协议）
openai

# For local development (reading .env files)
python-dotenv

# Time zone database for zoneinfo (needed on Windows / minimal images that lack
# a system tz database; used to date the report in REPORT_TIMEZONE, e.g. JST)
tzdata

# General purpose HTTP client (fallback)
requests